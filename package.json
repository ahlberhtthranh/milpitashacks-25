{
  "name": "milpitashacks-25",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "preview:worker": "npm run build && wrangler dev",
    "deploy:worker": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "vite": "^5.4.14",
    "wrangler": "^3.109.3"
  }
}
